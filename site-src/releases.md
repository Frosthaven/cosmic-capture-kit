---
title: Release notes
---

<!--
This page is GENERATED, and what is committed here is only a placeholder.

The docs deploy workflow rewrites this file from the project's published GitHub
releases every time the site is built, so the page that ships always matches the
releases that exist at that moment. The placeholder exists so that a local
`mkdocs build --strict` and `just docs` still have a page to build and the nav
entry still resolves. Editing it by hand changes nothing on the live site.
-->

# Release notes

The list of releases is generated when this site is deployed, so the published
page always matches the releases that exist right now.

Downloads live on the
[releases page](https://github.com/Frosthaven/cosmic-capture-kit/releases).
