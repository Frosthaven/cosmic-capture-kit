---
title: Cloud accounts
---

<!--
This page has no copy of its own on purpose.

`CLOUD_ACCOUNTS.md` in the repository root is the single source of truth for
the per-provider setup steps, and the include below pulls it in whole at build
time. Edit that file, never this one. If the include ever breaks, the build
fails loudly, because `pymdownx.snippets` runs with `check_paths: true`.
-->

--8<-- "CLOUD_ACCOUNTS.md"
