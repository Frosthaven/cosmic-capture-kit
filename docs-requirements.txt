# Pinned build requirements for the documentation site (mkdocs.yml + site-src/),
# published at https://cck.thedragon.dev
#
# Local preview:
#   python3 -m venv .venv-docs
#   .venv-docs/bin/pip install -r docs-requirements.txt
#   .venv-docs/bin/mkdocs serve        (or `just docs` once it is on PATH)
#
# Pinned exactly, so a local preview and the deploy job render the same site.
# Nothing here needs Rust; the site builds without touching the crate.
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-awesome-pages-plugin==2.10.1
pymdown-extensions==11.0.1
